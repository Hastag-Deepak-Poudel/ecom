First create a virtual environment and install the dependencies if you are not using docker container.

A ECOM with all the basic functionalities requrired.

admin username: dipak
admin password: ghost
